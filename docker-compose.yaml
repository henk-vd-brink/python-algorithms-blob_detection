version: "3"

services:
  detector:
    build:
      context: detector
      dockerfile: dockerfile
    ports:
      - 5000:5000
    environment:
      - NUMBER_OF_DETECTION_THREADS=1
      - DISABLE_JIT=1
    privileged: true
    depends_on:
      - streamer

  streamer:
    build:
      context: streamer
      dockerfile: dockerfile
    devices:
      - "/dev/video0"
    privileged: true
    volumes:
      - ./media:/media

    
    
    